void downloadExcelWeb(String fileName, List<int> bytes) {
  // Do nothing (not needed for mobile)
}

void downloadPDFWeb(String fileName, List<int> bytes) {
  // Do nothing (not needed for mobile)
}
